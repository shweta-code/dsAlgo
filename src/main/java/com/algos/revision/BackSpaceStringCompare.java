package com.algos.revision;

public class BackSpaceStringCompare {

    // https://leetcode.com/problems/backspace-string-compare/solution/
    // What is imp here ? One edge case -> if ((i >= 0) != (j >= 0))
    //                return false;
    // in the solution
}
